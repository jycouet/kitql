import { resolvers as _Mutation } from '../resolvers/_Mutation';
import { resolvers as _Query } from '../resolvers/_Query';
import { resolvers as _Scalar } from '../resolvers/_Scalar';

export const resolvers = [
  _Mutation,
  _Query,
  _Scalar,
];